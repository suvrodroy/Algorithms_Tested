#include "bits/stdc++.h"
using namespace std;
#define tableSize 10
typedef long long ll;
typedef long double ld;
struct hashTable{
    list<int> value;
};
int key(string s){
    int n = s.size();
    while()
}
int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    
    int n;
    string name;
    while(n--){
        cin>>name;

    }

    return 0;
}